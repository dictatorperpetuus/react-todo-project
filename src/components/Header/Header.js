import React from 'react';
import {Box} from "@mui/material";

const Header = () => {
    return (
        <Box sx={{textAlign: 'center'}}>
            <h1>Todo List</h1>
        </Box>
    );
};

export default Header;